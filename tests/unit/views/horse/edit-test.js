import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:horse/edit', 'HorseEditView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
